alias das=jump dot-a-shell
alias sm=showmarks

# Velocizzare ls
alias lsa=ls -a
alias lst=ls -t
alias ll=ls -l

# Configurazione colori in stile Nord Theme per a-Shell
alias nord=config -b #434C5E -f #8FBCBB && config -s 18 && config -n Menlo
